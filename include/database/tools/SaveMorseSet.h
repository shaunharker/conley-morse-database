#ifndef CMDB_SAVEMORSESET_H
#define CMDB_SAVEMORSESET_H

// Declaration
template < class Grid, class GridElement >
saveMorseSet (const char * filename,
	      const std::vector < GridElement > & set,
	      const Grid & grid );

// Definition
template < class Grid, class GridElement >
  saveMorseSet (const char * filename,
		const std::vector < GridElement > & set,
		const Grid & grid) {
  //TODO
}
	       

#endif
